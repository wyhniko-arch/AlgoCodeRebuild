# 2026.06.06 0:55
完成了对后端部分逻辑的重构，剩下一部分等前端写完再删，接下来要开始写前端然后搞前后端分离

我后端不定时传一个纯线性文本，结构就是扁平的 方法id-内容字符串，中间层拿到后根据id和注册表动态调用具体的方法并给它内容，具体的方法想怎么设计都可以，例如根据这些字符串进行解析，例如对它进一步进行方法id-内容的拆分和分发，实现高自由度渲染
我后端一开始那个返回运行上下文的部分改成封装一个这个，然后对于每个拿到字符串的方法如果直接去调用纯前端的方法肯定是不行的，这一步会分拆成微指令
这个绘制是否是一次性的脉冲，还是绘制就触发但是哈希标记为绘制过然后自己迭代作为画面效果，也是可以自己设计的
中间层根据一个列表是否为空而决定是否观察，观察时则根据规则拆分出很多 方法id-内容 字段，对于上次有过但这次没有的方法id，调用这种方法但传一个空串，然后就是无脑反射每个方法id的方法把字符串喂给它。每种方法根据每种方法自己的规则，去解读自己的字符串。对于未命中上次哈希的部分给予清除，这样如果传进来的是空串，自动清空自己这种类型的绘制。否则根据字符串，哈希命中则只改状态，之所以不无脑全删除上次的再绘制这次的是因为用于维护特效的迭代会置零
之所以可以不定时而不是高频，这实现的方式就是纯前端引擎的设计，状态不变时根据其指定的特效id自迭代
这样后端就不需要显式告诉前端什么东西的生命结束了



我不要你保留那些颜色字符串，你就不会发明一个新的数据结构来存储这些信息吗？你再看一遍，还有这个main也是的，
我让你封装一个可以根据指令去分发指令的东西在哪？main后面是要删掉的，这里面不可能超过10行，下面这段代码你没有完成保留后端逻辑的同时，
将那些信息，例如玩家当前语句的状态，以及下一步的词法分析表的状态，以及当前选中第几个的状态都分离出来，



其次给之前写的rowbuffer.java加上这个功能，即新增一种查询会返回当前所有的，但是会标记已经显示到的位置，
下次从这个位置开始往后，第二种是action:input:跟着输入，这部分的输入就是原来的要么单个字符，要么tab、del、enter这些，
你这些设计做了没？然后第三种，query:nextcommadpart，依然是返回一个文本，不过这个文本就是那个词法分析，这些人类随便都能想出来的方案在哪？
然后main里面可以暂时写一个输入，但是一定是a = 该开口(输入)这种，然后把a给打印出来，这才是我想要的方案，然后该东西就成为了一个黑盒，
第五种指令action:reset，相当于退出关卡，action:start:关卡号，相当于原来的进入关卡，然后你可以把那个传入的levelIndex给去掉啊，
然后在关卡期间才能输入action:input:……、query:nextcommadpart这种才有效，否则这种就无效，然后query:rowbuffer这是供我自己全局调试用的 

